# Data Notice

The CSV files in `data/raw/` originate from the coursework materials used for this project. Before publishing this repository publicly, review the module or institutional policy on redistribution of teaching data.

If redistribution is not permitted, keep the GitHub repository private or remove `data/raw/` before publishing. The scripts are written so that the data directory can be supplied externally with `--data-dir`.
